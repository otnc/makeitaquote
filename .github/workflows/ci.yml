name: ci (legacy)

on:
  push:
    branches: [legacy]
  pull_request:
    branches: [legacy]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      # `npm install` (vs `npm ci`) has a known bug with optional platform
      # deps (npm/cli#4828) when the lockfile was generated on a different
      # OS than the runner — tsdown/rolldown ship a native binding per
      # platform via optionalDependencies, so this must be a clean `npm ci`.
      - run: npm ci
      - run: npm run ci
      - run: npm run typecheck
      - run: npm run test
      - run: npm run build
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist

  # This branch targets consumers on older Node than main does (no native
  # bindings here), so verify the built dist/ actually loads under each
  # supported major, not just the Node 22 used to build/test it. No network
  # calls — that's the point, examples/ hits the live Voids API and is
  # deliberately not run here (see README's Development section).
  compat:
    needs: test
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: [18, 20, 22]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist
      - run: npm ci --omit=dev
      - run: node -e "require('./dist/index.cjs')"
      - run: node --input-type=module -e "import('./dist/index.mjs')"
