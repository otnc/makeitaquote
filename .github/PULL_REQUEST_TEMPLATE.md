## What does this change?

<!-- And why. Link the issue if there is one. -->

## Checklist

- [ ] `npm run ci` passes (Biome lint and format)
- [ ] `npm run typecheck` passes
- [ ] `npm run test` passes
- [ ] `npm run build && npm run check:build` passes, if the change touches
      exports, entry points or dependencies
- [ ] Tests cover the change
- [ ] README updated, if the public API changed

<!--
If this changes what an image looks like, please attach a before/after —
rendering output isn't something the test suite can meaningfully diff.
-->
